2. Downloads fail after minimizing the app. I see the following errors: Blocked by YouTube rate limited and No address associated with hostname.
4. Need support for different fileformats. I want to be able to import mp3 s to playlists for example.
5. Add shuffle toggle to the background play notification if possible.
6. Research if android auto integration is possible.
7. Ensure that playlists will get updated in the background on their schedule even if the user doesn't open the app.
8. Some videos fail with Skipping unsupported client "tv_embedded" let's roll this change back
9. add large videoplayer, automatically display it as landscape if it has that aspect ratio, even if the phone isn't rotated that way.